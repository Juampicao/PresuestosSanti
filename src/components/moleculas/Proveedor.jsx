import React from "react";

const Proveedor = ({ proveedor, setProveedor }) => {
  const { id, empresa, domicilio } = proveedor;

  return (
    <div>
      {/* <p> {id}</p>
      <p> {empresa} </p> */}
    </div>
  );
};

export default Proveedor;
